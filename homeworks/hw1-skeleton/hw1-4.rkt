#lang racket
(provide leaf node)
(provide is-empty-tree? is-leaf? leaf-val nth-child)

(define (leaf n)
  'TODO)

(define (node l)
  'TODO)

(define (is-empty-tree? t)
  'TODO)

(define (is-leaf? t)
  'TODO)

(define (leaf-val t)
  'TODO)

(define (nth-child t n)
  'TODO)
